import axios from 'axios';

/*
add additional actions with:
export const ${action_name} = '${action_name}';
*/

/*
add additional action functions with:
export const ${func_name} = (${args}) => dispatch => {};
*/
